Advanced calculator

Configure project using "cmake --preset". Available presets: default, debug-gdb, debug-lldb
Build project using "cmake --build -preset". Available presets: default, debug-gdb, debug-lldb
